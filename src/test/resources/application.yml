spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=3000
    username: testuser
    password: testpassword
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  flyway:
    url: jdbc:h2:mem:db
    user: testuser
    password: testpassword